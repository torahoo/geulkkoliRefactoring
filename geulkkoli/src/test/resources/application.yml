spring:
  profiles:
    active: test
  messages:
    basename: errors
  datasource:
    driver-class-name: org.h2.Driver
  h2:
    console:
      path: /h2-console
      #개인적을 h2-console을 사용하기 위한 설정입니다. 이는 지우거나 무시해주세요.
      #spring.datasource.url= jdbc:h2:tcp://localhost/~/testgg
      #spring.datasource.username= sa
      #spring.datasource.password=
      #spring.datasource.driver-class-name= org.h2.Driver
      #
      #
      #spring.jpa.hibernate.ddl-auto= create-drop


  #587 포트가 이메일 전송에 유용한 보안 포트입니다.
  #구글 계정으로 앱 서비스를 설정하여 전송할 수 있게 했습니다.
  #username은 우리가 이메일 전송 시 사용하는 계정입니다.
  #설정해 둔 계정으로 이메일을 전송하는데, JavaMailSender 클래스를 통해 작동합니다. (이메일 서비스에 불러와져 있는 그거)
  mail:
    host: smtp.gmail.com # 서버 호스트
    port: 587 # 서버 포트
    username: daejanggoyangi
    password: vphabcofycwufewh # 실제 비밀번호가 아니라 앱 서비스용 비밀번호라 구글 로그인은 안 됨
    properties:
      mail:
        smtp:
          auth: true # 사용자 인증 시도 여부 (기본값 : false)
          timeout: 5000 # Socket Read Timeout 시간(ms) (기본값 : 무한대)
          starttls:
            enable: true # StartTLS 활성화 여부 (기본값 : false)

#JPA log
logging:
  level:
    org:
      hibernate:
        SQL: debug
        type:
          descriptor:
            sql:
              BasicBinder: trace
server:
  error:
    whitelabel:
      enabled: false
    path: /error
    include-exception: false
    include-message: never
